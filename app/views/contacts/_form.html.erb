<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-inputs-container">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, class: 'input-text' %>
    </div>

    <div class="field">
      <%= form.label :last_name %>
      <%= form.text_field :last_name, class: 'input-text' %>
    </div>

    <div class="field">
      <%= form.label :phone %>
      <%= form.text_field :phone, class: 'input-text' %>
    </div>

    <div class="field">
      <%= form.label :company %>
      <%= form.text_field :company, class: 'input-text' %>
    </div>

    <div class="field">
      <%= form.label :email %>
      <%= form.text_field :email, class: 'input-text' %>
    </div>
  </div>
  <div class="actions">
    <%= form.submit class: 'btn-success' %>
  </div>
<% end %>
